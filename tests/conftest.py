"""Test configuration.

The implementation lives in the installed ``social_media_agent`` package
(``pip install -e .``), so no sys.path manipulation is needed — tests import
``from social_media_agent.<subpkg>...`` directly.
"""
